/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory();
	else if(typeof define === 'function' && define.amd)
		define([], factory);
	else {
		var a = factory();
		for(var i in a) (typeof exports === 'object' ? exports : root)[i] = a[i];
	}
})(self, () => {
return /******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ "./index.ts":
/*!******************!*\
  !*** ./index.ts ***!
  \******************/
/***/ ((__unused_webpack_module, exports) => {

eval("{\n// -----------------------------------------------------------------------------\n// TYPES\n// -----------------------------------------------------------------------------\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.AudioElementLoader = exports.AudioBufferLoader = exports.AudioManager = exports.AudioItem = exports.AudioStatus = exports.PlayMode = void 0;\nvar PlayMode;\n(function (PlayMode) {\n    PlayMode[\"Trigger\"] = \"trigger\";\n    PlayMode[\"Retrigger\"] = \"retrigger\";\n    PlayMode[\"Hold\"] = \"hold\";\n})(PlayMode = exports.PlayMode || (exports.PlayMode = {}));\nvar AudioStatus;\n(function (AudioStatus) {\n    AudioStatus[\"Stopped\"] = \"stopped\";\n    AudioStatus[\"Playing\"] = \"playing\";\n    AudioStatus[\"Paused\"] = \"paused\";\n})(AudioStatus = exports.AudioStatus || (exports.AudioStatus = {}));\n// -----------------------------------------------------------------------------\n// CLASSES\n// -----------------------------------------------------------------------------\nclass AudioItem {\n    constructor(audioContext, options) {\n        this.isSourceConnected = false;\n        this._hold = false;\n        this._status = AudioStatus.Stopped;\n        this.mode = AudioItem.DEFAULT_OPTIONS.mode;\n        this.volume = AudioItem.DEFAULT_OPTIONS.volume;\n        this.pan = AudioItem.DEFAULT_OPTIONS.pan;\n        this.pitch = AudioItem.DEFAULT_OPTIONS.pitch;\n        this.loop = AudioItem.DEFAULT_OPTIONS.loop;\n        const actualOptions = Object.assign({}, AudioItem.DEFAULT_OPTIONS, options);\n        this.audioContext = audioContext;\n        this.group = actualOptions.group;\n        this.mode = actualOptions.mode;\n        this.volume = actualOptions.volume;\n        this.pan = actualOptions.pan;\n        this.pitch = actualOptions.pitch;\n        this.loop = actualOptions.loop;\n        this.onStart = actualOptions.onStart;\n        this.onFinish = actualOptions.onFinish;\n        this.onRepeat = actualOptions.onRepeat;\n        // Create audio nodes\n        this.gainNode = audioContext.createGain();\n        this.panNode = audioContext.createStereoPanner();\n        // Connect nodes\n        this.gainNode.connect(this.panNode);\n        this.panNode.connect(audioContext.destination);\n        // Set up audio source\n        if (actualOptions.audio instanceof AudioBuffer) {\n            this.audioBuffer = actualOptions.audio;\n        }\n        else {\n            this.audioElement = actualOptions.audio;\n            // Create the MediaElementAudioSourceNode once for HTMLAudioElement\n            if (this.audioElement.src) {\n                this.mediaElementSource = this.audioContext.createMediaElementSource(this.audioElement);\n            }\n        }\n        this.updateAudioProperties();\n    }\n    updateAudioProperties() {\n        this.gainNode.gain.value = this.volume;\n        this.panNode.pan.value = this.pan;\n        // Apply pitch based on pitch mode\n        this.applyPitch();\n    }\n    applyPitch() {\n        if (this.source instanceof AudioBufferSourceNode) {\n            this.source.playbackRate.value = this.pitch;\n        }\n        else if (this.audioElement) {\n            this.audioElement.playbackRate = this.pitch;\n        }\n    }\n    createSource() {\n        if (this.source && this.isSourceConnected) {\n            this.disconnectSource();\n        }\n        if (this.audioBuffer) {\n            // AudioBufferSourceNode can only be used once, so create a new one\n            // each time\n            const bufferSource = this.audioContext.createBufferSource();\n            bufferSource.buffer = this.audioBuffer;\n            bufferSource.loop = this.loop;\n            bufferSource.onended = () => {\n                var _a, _b;\n                if (this.loop && this._status === AudioStatus.Playing) {\n                    (_a = this.onRepeat) === null || _a === void 0 ? void 0 : _a.call(this);\n                }\n                else {\n                    this._status = AudioStatus.Stopped;\n                    (_b = this.onFinish) === null || _b === void 0 ? void 0 : _b.call(this);\n                }\n            };\n            this.source = bufferSource;\n        }\n        else if (this.audioElement) {\n            if (!this.audioElement.src) {\n                return;\n            }\n            // Reuse the existing MediaElementAudioSourceNode\n            if (!this.mediaElementSource) {\n                this.mediaElementSource = this.audioContext.createMediaElementSource(this.audioElement);\n            }\n            this.audioElement.loop = this.loop;\n            // Set up the onended callback on the audio element\n            this.audioElement.onended = () => {\n                var _a, _b;\n                if (this.loop && this._status === AudioStatus.Playing) {\n                    (_a = this.onRepeat) === null || _a === void 0 ? void 0 : _a.call(this);\n                }\n                else {\n                    this._status = AudioStatus.Stopped;\n                    (_b = this.onFinish) === null || _b === void 0 ? void 0 : _b.call(this);\n                }\n            };\n            this.source = this.mediaElementSource;\n        }\n        if (this.source) {\n            this.source.connect(this.gainNode);\n            this.isSourceConnected = true;\n            // Apply pitch after source is connected\n            this.applyPitch();\n        }\n    }\n    disconnectSource() {\n        if (this.source && this.isSourceConnected) {\n            this.source.disconnect();\n            this.isSourceConnected = false;\n            // For AudioBufferSourceNode, clear the reference since it can't be reused\n            if (this.source instanceof AudioBufferSourceNode) {\n                this.source = undefined;\n            }\n            // For MediaElementAudioSourceNode, keep the reference for reuse\n        }\n    }\n    get status() {\n        return this._status;\n    }\n    get hold() {\n        return this._hold;\n    }\n    set hold(value) {\n        this._hold = value;\n        if (this.mode === 'hold') {\n            if (value && this._status !== AudioStatus.Playing) {\n                this.play();\n            }\n            else if (!value && this._status === AudioStatus.Playing) {\n                this.stop();\n            }\n        }\n    }\n    play() {\n        var _a;\n        if (this.mode === 'trigger' && this._status === AudioStatus.Playing) {\n            return;\n        }\n        if (this.mode === 'retrigger' && this._status === AudioStatus.Playing) {\n            this.stop();\n        }\n        if (this.mode === 'hold' && !this._hold) {\n            return;\n        }\n        this.createSource();\n        if (this.source instanceof AudioBufferSourceNode) {\n            this.source.start();\n        }\n        else if (this.audioElement) {\n            this.audioElement.currentTime = 0;\n            this.audioElement.play();\n        }\n        this._status = AudioStatus.Playing;\n        (_a = this.onStart) === null || _a === void 0 ? void 0 : _a.call(this);\n    }\n    fadeIn(duration) {\n        if (this.fadeTimeout) {\n            clearTimeout(this.fadeTimeout);\n        }\n        const originalVolume = this.volume;\n        this.gainNode.gain.value = 0;\n        this.play();\n        const startTime = this.audioContext.currentTime;\n        this.gainNode.gain.linearRampToValueAtTime(originalVolume, startTime + duration);\n        this.fadeTimeout = window.setTimeout(() => {\n            this.gainNode.gain.value = originalVolume;\n        }, duration * 1000);\n    }\n    fadeOut(duration) {\n        if (this.fadeTimeout) {\n            clearTimeout(this.fadeTimeout);\n        }\n        if (this._status !== AudioStatus.Playing) {\n            return;\n        }\n        const startTime = this.audioContext.currentTime;\n        this.gainNode.gain.linearRampToValueAtTime(0, startTime + duration);\n        this.fadeTimeout = window.setTimeout(() => {\n            this.stop();\n            this.gainNode.gain.value = this.volume;\n        }, duration * 1000);\n    }\n    stop() {\n        if (this._status === AudioStatus.Stopped) {\n            return;\n        }\n        if (this.fadeTimeout) {\n            clearTimeout(this.fadeTimeout);\n            this.fadeTimeout = undefined;\n        }\n        if (this.source instanceof AudioBufferSourceNode) {\n            this.source.stop();\n        }\n        else if (this.audioElement) {\n            this.audioElement.pause();\n            this.audioElement.currentTime = 0;\n        }\n        this._status = AudioStatus.Stopped;\n        this.disconnectSource();\n    }\n    pause() {\n        if (this._status !== AudioStatus.Playing) {\n            return;\n        }\n        if (this.audioElement) {\n            this.audioElement.pause();\n        }\n        else {\n            // For AudioBuffer sources, we need to stop and remember position\n            this.stop();\n        }\n        this._status = AudioStatus.Paused;\n    }\n    resume() {\n        if (this._status !== AudioStatus.Paused) {\n            return;\n        }\n        if (this.audioElement) {\n            this.audioElement.play();\n            this._status = AudioStatus.Playing;\n        }\n        else {\n            // For AudioBuffer sources, we need to restart from beginning\n            this.play();\n        }\n        this._status = AudioStatus.Playing;\n    }\n    reset() {\n        this.stop();\n        if (this.audioElement) {\n            this.audioElement.currentTime = 0;\n        }\n    }\n    updateProperties() {\n        this.updateAudioProperties();\n    }\n}\nexports.AudioItem = AudioItem;\nAudioItem.DEFAULT_OPTIONS = {\n    mode: PlayMode.Trigger,\n    volume: 1,\n    pan: 0,\n    pitch: 1,\n    loop: false,\n};\nclass AudioManager {\n    constructor() {\n        this.sounds = new Map();\n        this._globalVolume = 1;\n        this._globalPan = 0;\n        this._globalPitch = 1;\n        this._globalMuted = false;\n        this.groupSettings = new Map();\n        this.audioContext = new (window.AudioContext ||\n            window.webkitAudioContext)();\n    }\n    get globalVolume() {\n        return this._globalVolume;\n    }\n    get globalPan() {\n        return this._globalPan;\n    }\n    get globalPitch() {\n        return this._globalPitch;\n    }\n    get globalMuted() {\n        return this._globalMuted;\n    }\n    static getDefaultGroupSettings() {\n        return {\n            ...AudioManager.DEFAULT_GROUP_SETTINGS,\n        };\n    }\n    static initialise(soundsConfig) {\n        if (!AudioManager.instance) {\n            AudioManager.instance = new AudioManager();\n        }\n        for (const [name, options] of Object.entries(soundsConfig)) {\n            AudioManager.add(name, options);\n        }\n    }\n    static get sounds() {\n        if (!AudioManager.instance) {\n            throw new Error('AudioManager not initialised');\n        }\n        const sounds = {};\n        AudioManager.instance.sounds.forEach((sound, name) => {\n            sounds[name] = sound;\n        });\n        return sounds;\n    }\n    static add(name, options) {\n        if (!AudioManager.instance) {\n            throw new Error('AudioManager not initialised');\n        }\n        const sound = new AudioItem(AudioManager.instance.audioContext, options);\n        AudioManager.instance.sounds.set(name, sound);\n    }\n    static remove(name) {\n        if (!AudioManager.instance) {\n            throw new Error('AudioManager not initialised');\n        }\n        const sound = AudioManager.instance.sounds.get(name);\n        if (sound) {\n            sound.stop();\n            AudioManager.instance.sounds.delete(name);\n        }\n    }\n    static setVolume(volume, groupName) {\n        if (!AudioManager.instance) {\n            throw new Error('AudioManager not initialised');\n        }\n        if (groupName) {\n            const settings = AudioManager.instance.groupSettings.get(groupName) ||\n                AudioManager.getDefaultGroupSettings();\n            settings.volume = volume;\n            AudioManager.instance.groupSettings.set(groupName, settings);\n            AudioManager.instance.sounds.forEach(sound => {\n                if (sound.group === groupName) {\n                    sound.volume = volume;\n                    sound.updateProperties();\n                }\n            });\n        }\n        else {\n            AudioManager.instance._globalVolume = volume;\n            AudioManager.instance.sounds.forEach(sound => {\n                if (!sound.group) {\n                    sound.volume = volume;\n                    sound.updateProperties();\n                }\n            });\n        }\n    }\n    static setPan(pan, groupName) {\n        if (!AudioManager.instance) {\n            throw new Error('AudioManager not initialised');\n        }\n        if (groupName) {\n            const settings = AudioManager.instance.groupSettings.get(groupName) ||\n                AudioManager.getDefaultGroupSettings();\n            settings.pan = pan;\n            AudioManager.instance.groupSettings.set(groupName, settings);\n            AudioManager.instance.sounds.forEach(sound => {\n                if (sound.group === groupName) {\n                    sound.pan = pan;\n                    sound.updateProperties();\n                }\n            });\n        }\n        else {\n            AudioManager.instance._globalPan = pan;\n            AudioManager.instance.sounds.forEach(sound => {\n                if (!sound.group) {\n                    sound.pan = pan;\n                    sound.updateProperties();\n                }\n            });\n        }\n    }\n    static setPitch(pitch, groupName) {\n        if (!AudioManager.instance) {\n            throw new Error('AudioManager not initialised');\n        }\n        if (groupName) {\n            const settings = AudioManager.instance.groupSettings.get(groupName) ||\n                AudioManager.getDefaultGroupSettings();\n            settings.pitch = pitch;\n            AudioManager.instance.groupSettings.set(groupName, settings);\n            AudioManager.instance.sounds.forEach(sound => {\n                if (sound.group === groupName) {\n                    sound.pitch = pitch;\n                    sound.updateProperties();\n                }\n            });\n        }\n        else {\n            AudioManager.instance._globalPitch = pitch;\n            AudioManager.instance.sounds.forEach(sound => {\n                if (!sound.group) {\n                    sound.pitch = pitch;\n                    sound.updateProperties();\n                }\n            });\n        }\n    }\n    static setMuted(muted, groupName) {\n        if (!AudioManager.instance) {\n            throw new Error('AudioManager not initialised');\n        }\n        if (groupName) {\n            const settings = AudioManager.instance.groupSettings.get(groupName) ||\n                AudioManager.getDefaultGroupSettings();\n            settings.muted = muted;\n            AudioManager.instance.groupSettings.set(groupName, settings);\n            AudioManager.instance.sounds.forEach(sound => {\n                if (sound.group === groupName) {\n                    // Implement muting by setting gain to 0\n                    const gainNode = sound.gainNode;\n                    gainNode.gain.value = muted ? 0 : sound.volume;\n                }\n            });\n        }\n        else {\n            AudioManager.instance._globalMuted = muted;\n            AudioManager.instance.sounds.forEach(sound => {\n                if (!sound.group) {\n                    const gainNode = sound.gainNode;\n                    gainNode.gain.value = muted ? 0 : sound.volume;\n                }\n            });\n        }\n    }\n    static playAll(groupName) {\n        if (!AudioManager.instance) {\n            throw new Error('AudioManager not initialised');\n        }\n        AudioManager.instance.sounds.forEach(sound => {\n            if (!groupName || sound.group === groupName) {\n                sound.play();\n            }\n        });\n    }\n    static pauseAll(groupName) {\n        if (!AudioManager.instance) {\n            throw new Error('AudioManager not initialised');\n        }\n        AudioManager.instance.sounds.forEach(sound => {\n            if (!groupName || sound.group === groupName) {\n                sound.pause();\n            }\n        });\n    }\n    static resumeAll(groupName) {\n        if (!AudioManager.instance) {\n            throw new Error('AudioManager not initialised');\n        }\n        AudioManager.instance.sounds.forEach(sound => {\n            if ((!groupName || sound.group === groupName) &&\n                sound.status === AudioStatus.Paused) {\n                sound.resume();\n            }\n        });\n    }\n    static stopAll(groupName) {\n        if (!AudioManager.instance) {\n            throw new Error('AudioManager not initialised');\n        }\n        AudioManager.instance.sounds.forEach(sound => {\n            if (!groupName || sound.group === groupName) {\n                sound.stop();\n            }\n        });\n    }\n    static resetAll(groupName) {\n        if (!AudioManager.instance) {\n            throw new Error('AudioManager not initialised');\n        }\n        AudioManager.instance.sounds.forEach(sound => {\n            if (!groupName || sound.group === groupName) {\n                sound.reset();\n            }\n        });\n    }\n    // Static convenience methods for individual sounds\n    static play(name) {\n        const sound = AudioManager.sounds[name];\n        if (sound) {\n            sound.play();\n        }\n    }\n    static fadeIn(name, duration) {\n        const sound = AudioManager.sounds[name];\n        if (sound) {\n            sound.fadeIn(duration);\n        }\n    }\n    static fadeOut(name, duration) {\n        const sound = AudioManager.sounds[name];\n        if (sound) {\n            sound.fadeOut(duration);\n        }\n    }\n    static stop(name) {\n        const sound = AudioManager.sounds[name];\n        if (sound) {\n            sound.stop();\n        }\n    }\n    static pause(name) {\n        const sound = AudioManager.sounds[name];\n        if (sound) {\n            sound.pause();\n        }\n    }\n    static resume(name) {\n        const sound = AudioManager.sounds[name];\n        if (sound) {\n            sound.resume();\n        }\n    }\n    static reset(name) {\n        const sound = AudioManager.sounds[name];\n        if (sound) {\n            sound.reset();\n        }\n    }\n}\nexports.AudioManager = AudioManager;\nAudioManager.DEFAULT_GROUP_SETTINGS = {\n    volume: 1,\n    pan: 0,\n    pitch: 1,\n    muted: false,\n};\n// -----------------------------------------------------------------------------\n// CONTENT LOADER\n// -----------------------------------------------------------------------------\n/**\n * Content Manager Loader for loading an audio file as an AudioBuffer\n *\n * Note: This loader requires a web server (http/https) to work properly due to\n * browser security restrictions. For loading audio from the local filesystem\n * (file://), use AudioElementLoader instead.\n *\n * @see https://www.npmjs.com/package/@basementuniverse/content-manager\n */\nconst AudioBufferLoader = async (url) => {\n    return new Promise(async (resolve, reject) => {\n        try {\n            const response = await fetch(url);\n            const arrayBuffer = await response.arrayBuffer();\n            const audioContext = new (window.AudioContext ||\n                window.webkitAudioContext)();\n            const audioBuffer = await audioContext.decodeAudioData(arrayBuffer);\n            resolve(audioBuffer);\n        }\n        catch (error) {\n            reject(`Error loading audio buffer \"${url}\": ${error}`);\n        }\n    });\n};\nexports.AudioBufferLoader = AudioBufferLoader;\n/**\n * Content Manager Loader for loading an audio file as an HTMLAudioElement\n *\n * Note: This loader works reliably with both web servers (http/https) and local\n * filesystem (file://), similar to how the ImageLoader works. Use this loader\n * when you need to load audio files from the local filesystem.\n *\n * @see https://www.npmjs.com/package/@basementuniverse/content-manager\n */\nconst AudioElementLoader = async (url) => {\n    return new Promise((resolve, reject) => {\n        const audio = new Audio();\n        audio.src = url;\n        audio.addEventListener('canplaythrough', () => {\n            resolve(audio);\n        });\n        audio.addEventListener('error', () => {\n            reject(`Error loading audio element \"${url}\"`);\n        });\n    });\n};\nexports.AudioElementLoader = AudioElementLoader;\n\n\n//# sourceURL=webpack://@basementuniverse/audio-manager/./index.ts?\n}");

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module is referenced by other modules so it can't be inlined
/******/ 	var __webpack_exports__ = {};
/******/ 	__webpack_modules__["./index.ts"](0, __webpack_exports__);
/******/ 	
/******/ 	return __webpack_exports__;
/******/ })()
;
});